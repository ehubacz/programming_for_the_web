const {createApp} = Vue;

createApp({
    data() {
        return {
            name: "Emma"
        }
    }
}).mount("#myApp");

createApp ({
    data() {
        return {
            name:"Foureyes"
        }
    }
}).mount("#hatersApp")
